#!/usr/bin/python3
""" Module 0-lookup
Returns a list of the attributes and methods of an object
"""


def lookup(obj):
    """ Returns the lists of attributes and methods of object obj
    Args:
        obj (_type_): object
    Returns:
        dir(obj)
    """
    return (dir(obj))
